# HuggingFace Inference API (optional — raises rate limits for cross-encoder rerank)
HF_TOKEN=your-HF_TOKEN
# Alias also accepted by the package
# HUGGINGFACE_API_KEY=

CROSS_ENCODER_MODEL=cross-encoder/ms-marco-MiniLM-L6-v2
